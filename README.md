This repo contains code for solving DesertKing problem using Dinkelbach approach. 
It is implemented in C++ with the aid of priority-queue for run time complexity of O(NlogN). The memory complexity is O(N^2) due to the use of adjacency matrix.
